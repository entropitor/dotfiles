if [ -f ~/.aliases.local ]; then
    . ~/.aliases.local
fi

alias gdf="git dsf"
alias vi="vim"
alias nr="npm run"
alias nx="npx"
alias yr="yarn run"
if [ -x "$(command -v htop)" ]; then
  alias top="htop"
fi
if [ -x "$(command -v bat)" ]; then
  alias cat="bat"
fi
